# Woke CLI

### Usage

```
Woke Contracts CLI v0.1.0
Usage:
  getTweetText <userId>
  getLodgedTweets <userId>
  getUser <userId>
  getClaimEvents [userId, address]
  getTransferEvents [[from,to] <userId>]
  getBonusEvents [[claimer,referrer] <userId>]
  supply [minted]
```

