{
  "name": "@woke/oracle",
  "version": "0.1.0",
  "description": "Tiny oracle for Twitter API to avoid waiting for Oraclize",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "ETH_ENV=development ./node_modules/.bin/mocha test/*.test.js",
    "test:debug": "DEBUG=*,test:*,-mocha*,-*:info ETH_ENV=development ./node_modules/.bin/mocha test/*.test.js",
    "start": "NODE_ENV=production ETH_ENV=production WOKE_ROLE=oracle DEBUG=* node src/index.js",
    "start:docker": "ETH_ENV=development NODE_ENV=development CONTEXT=local DEBUG_RECOVERY=true DEBUG=* node src/index.js",
    "start:docker:dev": "docker-compose -f ../../oracle.docker-compose.local.yml up",
    "dev": "NODE_ENV=development ETH_ENV=development PERSIST=false CONTEXT=local DEBUG=*,twitter node src/index.js",
    "goerli": "CONTEXT=local NODE_ENV=development ETH_ENV=goerli DEBUG=*,twitter node src/index.js",
    "dev:with_client": "DEBUG=oracle:* node src/lib/client.js",
    "docker:db": "docker-compose -f ../../oracle.docker-compose.local.yml up -d --build oracle-db",
    "docker:down": "docker-compose -f ../../oracle.docker-compose.local.yml down",
    "docker:reset": "docker-compose -f ../../oracle.docker-compose.local.yml down -v"
  },
  "author": "Jackson Virgo",
  "license": "ISC",
  "dependencies": {
    "@woke/web3-nact": "0.1.0",
    "@woke/contracts": "0.4.0",
    "@woke/lib": "0.4.0",
    "@woke/actors": "0.1.0",
    "@woke/twitter": "0.1.0",
    "@woke/wact": "0.1.0",
    "@woke/test": "0.1.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "minimist": "^1.2.0",
    "mocha": "^6.2.0",
    "web3": "^1.2.9"
  }
}
